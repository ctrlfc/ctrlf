/**
 * @file task0002_2
 * @author ctrlf
 */

/**
 * 对目标字符串格式化
 * @param {string} [source] [目标字符串]
 * @param {Object|string} [opts] [提供相应字符串对象或者多个字符串]
 *
 */

function format(source, opts) {
	source = String(source);
	 var date = Array.prototype.slice.call(arguments,
	 	if (data.length) {
	 		// statement
	 	}
	 	)
}
